services:
  - type: web
    name: wavecast-server
    runtime: node
    rootDir: .
    buildCommand: npm install --workspaces --include-workspace-root && npx prisma generate
    startCommand: cd server && npx tsx src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
